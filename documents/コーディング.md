# コーディング

## DB
### GET
- 呼び出し元
```
app/nextjsapp/src/app/page.tsx
```
- 呼び出し
```
const [user, setUser] = useState<User>({
    id: 0,
    number: '',
    familyname: '',
    firstname: '',
    email: '',
    img: '',
})

useEffect(() => {
  fetch('/api/users/?number=30305637')
    .then((res) => res.json())
    .then((data) => setUser(data))
}, [])
```
- APIのパス
```
app/nextjsapp/src/app/api/users/route.ts
```
- API
```
export async function GET(request: NextRequest) {
  const searchParams = request.nextUrl.searchParams
  const query = searchParams.get('number')
  let number: string = query ? query : ''

  const user = await prisma.user.findUnique({
    where: {
      number: number,
    },
  })

  return Response.json(user)
}
```

## 参考
[GETのパラメータ取得](https://nextjs.org/docs/app/api-reference/file-conventions/route).  
